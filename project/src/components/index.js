import Features from "./Features";
import Download from "./Download";
import SectionWrapper from "./SectionWrapper";

export { SectionWrapper, Download, Features };

/* you might wonder why we didn't import the Button, 
because we will use the Button in these exported components, 
not in the app file directly
*/
